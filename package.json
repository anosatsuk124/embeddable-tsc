{
    "name": "embeddable-tsc",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "build": "tsc && browserify dist/* -o tsc.js",
        "check": "tsc --noEmit",
        "fmt": "prettier --write ."
    },
    "devDependencies": {
        "browserify": "^17.0.0",
        "prettier": "^2.7.1"
    },
    "dependencies": {
        "typescript": "^4.8.4"
    }
}
